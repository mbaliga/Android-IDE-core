Scenario (FB-RAT-AUTH-005 "purpose and target binding"):

fixtures/authority/valid/grant-secret-use-purpose-bound-valid.json (grantId
grant-secret-use-asom-pairing-0003) authorizes fb.secret.use with
constraints.purpose="asom-model-provider-authentication" and
resourceScope={kind: NETWORK_DOMAIN, locator: "127.0.0.1:11435"} — it exists so
principal-local-agent-persona-primary can authenticate to the on-device ASOM daemon
(03_CONSTELLATION_CONTEXT addendum: a purpose-bound network+secret capability, AIDL-paired, NOT a
general network grant).

This decision record shows the SAME principal, using that SAME grantId, but for a request whose
requestedResourceScope is {kind: EXECUTION_TARGET, locator: "target-local-android-this-phone"} —
i.e. attempting to hand the ASOM-pairing secret to a local shell/process operation instead of the
network destination it was purpose-bound to. The record is schema-valid on its own: DENY does not
require matchedGrantId to be null, and every field individually validates. jsonschema structural
validation has no way to compare THIS record's requestedResourceScope against the REFERENCED
grant's resourceScope/constraints.purpose across two separate documents — that cross-object
comparison is exactly the check a real policy engine must perform.

Required real-implementation behavior:
- MUST resolve matchedGrantId to the actual Grant object and compare both resourceScope.kind and
  constraints.purpose against the request's own claimed intended use before returning ALLOW.
- MUST return outcome=DENY, reasonCode=AUTHORITY_PURPOSE_MISMATCH (or
  AUTHORITY_TARGET_MISMATCH, if the engine treats the resourceScope.kind disagreement as the
  primary failure) the moment either axis disagrees — purpose-binding and target-binding are two
  independent checks per FB-RAT-AUTH-005, and either failing alone is sufficient to deny.
- MUST NOT let a secret handle purpose-bound to one destination (a model provider) become usable
  for an unrelated destination (a shell/network operation) merely because the same principal holds
  both, and MUST NOT ever return the raw secret value into this decision-record's evidence trail
  (see the parallel secret-leak fixtures at fixtures/common/adversarial/error-secret-leak-in-detail
  and fixtures/execution/adversarial/receipt-secret-leak-in-log-excerpt for the sibling obligation
  that a secret's raw value is never logged).
