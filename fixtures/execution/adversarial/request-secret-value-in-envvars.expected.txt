Structural validation result: PASSES schemas/execution/request.schema.json.
`environment.envVars` is only constrained to be a map of string -> string — JSON Schema has no
way to know a value is a live API key rather than an ordinary non-secret setting like
`GRADLE_OPTS`. This fixture is adversarial at the semantic layer only.

MUST BE REJECTED BY A REAL IMPLEMENTATION — this fixture is a negative example, not a template.

Required behavior from a real implementation (not just structural validation):
  1. A caller MUST NOT place a secret value directly in `environment.envVars`. FB-RAT-EXE-007
     ("requests reference purpose-bound secret handles; logs/receipts never contain secret
     values") and the binding API-key rule (CLAUDE.md rule 5: "encrypted at rest via Android
     Keystore, never logged, never sent anywhere except the provider they belong to") require the
     caller to instead populate `secretHandles` with a `{handleId, purpose}` entry and have the
     PROVIDER resolve the handle to a real value at the last possible moment, outside this
     contract's own serialized form.
  2. A provider (or an authority-engine gate sitting in front of it, per FB-RAT-COM-012 "no
     contract bypass") that receives an ExecutionRequest MUST scan `environment.envVars` for
     credential-shaped values (the same pattern class as
     fixtures/common/adversarial/error-secret-leak-in-detail.adversarial.json) and REFUSE to
     start the operation rather than silently forwarding a live key into `operation.command`'s
     shell interpolation, a log excerpt, or an ExecutionReceipt.
  3. Recommended ErrorEnvelope for the refusal (schemas/common/error.schema.json):
       code: "EXECUTION_SECRET_VALUE_IN_ENVIRONMENT"
       severity: "CRITICAL"
       retryable: false
       sideEffectState: "NONE"
       recoveryAction: "Replace the raw value in environment.envVars with a secretHandles entry
         referencing a Keystore-backed handle for the same purpose, then resubmit."
       userMessage: "This request was refused because it tried to pass a credential as a plain
         setting instead of a secure handle."
  4. This check MUST happen before dispatch to the target (LOCAL_ANDROID shell, SSH exec, or CI
     dispatch payload) — refuse first, never send-then-audit, since the secret would already have
     left the device by the time any post-hoc audit ran.
