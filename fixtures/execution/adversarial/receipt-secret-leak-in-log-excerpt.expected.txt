Structural validation result: PASSES schemas/execution/receipt.schema.json.
`logs.excerpt` is only constrained to be a string of at most 4096 characters, and
`logs.redactionApplied` is only constrained to be a boolean — JSON Schema has no way to know a
substring of `excerpt` is a live credential, or that `redactionApplied: true` is a false claim
about that same string. This fixture is adversarial at the semantic layer only, exactly mirroring
fixtures/common/adversarial/error-secret-leak-in-detail.adversarial.json's shape for
ErrorEnvelope.detail, applied here to ExecutionReceipt.logs.excerpt.

MUST BE REJECTED BY A REAL IMPLEMENTATION — this fixture is a negative example, not a template.

Required behavior from a real implementation (not just structural validation):
  1. Before an ExecutionReceipt carrying a provider request/response log excerpt is constructed,
     the producer MUST scan and redact credential-shaped substrings from `logs.excerpt` (and from
     whatever `logs.ref` points at) — e.g. patterns like `ghp_[A-Za-z0-9]{20,}`,
     `Bearer [A-Za-z0-9._-]{20,}`, `sk-[A-Za-z0-9-]{20,}`, `x-api-key:\s*\S+` — per the binding
     rule "API keys ... never logged" (CLAUDE.md rule 5) and FB-RAT-EXE-007 (secret handles;
     logs/receipts never contain secret values). A technical-detail/log-excerpt field is not an
     exemption from that rule.
  2. `logs.redactionApplied: true` on a receipt whose `excerpt` still contains a credential-shaped
     substring is a producer bug, not a merely-suboptimal receipt — a conformance ADVERSARIAL test
     for ExecutionReceipt MUST include a fixture exactly like this one and assert the producer path
     REJECTS constructing the receipt, or re-scans and redacts, before persistence/transmission.
  3. Recommended remediation: replace the credential substring with a fixed redaction marker
     (e.g. "[REDACTED-TOKEN]") and keep the rest of `excerpt` intact — see the compliant
     counterpart fixtures/execution/valid/receipt-log-excerpt-redacted-clean.json, which describes
     the same failure class (a 401 against the GitHub dispatch API) but names the secret HANDLE
     (`01J8ZH0000000000000000S2`) instead of any token value.
  4. This applies regardless of `exitState` or `verification.state` — redaction happens before
     those fields are even considered, and applies identically whether the receipt reports success
     or failure (a failed auth attempt's log is exactly where a raw credential is most likely to
     appear verbatim in provider error text, making this the highest-risk case, not an edge case).
  5. Recommended ErrorEnvelope for the underlying auth failure this receipt's request would also
     produce (schemas/common/error.schema.json):
       code: "GIT_AUTH_EXPIRED" or "CLOUD_PROVIDER_REQUEST_FAILED"
       detail: MUST be redacted using the same rule as this receipt's `logs.excerpt` — the two
         redaction obligations are the same obligation applied to two different contract shapes,
         not two independent rules to satisfy separately.
