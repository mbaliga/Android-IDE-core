Structural validation result: PASSES schemas/thread/thread-event.schema.json. `chosenRef` and
`alternatives[]` are only constrained to be strings — JSON Schema has no way to know a substring
is a live API key, so this fixture is adversarial at the semantic layer only (same class of gap
fixtures/common/adversarial/error-secret-leak-in-detail.adversarial.json documents for
ErrorEnvelope.detail).

Required behavior from a real implementation (not just structural validation):
  1. `chosenRef` names WHAT was chosen (a model/branch/edge-label id) — it MUST NEVER be built by
     interpolating a full model-selector string that embeds `api_key=`/`Authorization: Bearer `/
     any other credential-shaped substring. Per CLAUDE.md binding rule 5 ("API keys ... never
     logged") and rule 2 (cloud providers are provider-generic "watched objects", never holding
     the caller's own key material in a display/log-shaped field), a DELEGATION event's
     `chosenRef`/`alternatives` MUST resolve to an opaque model/branch id (e.g. the same id
     `dev.aarso.data.ProviderStore` already uses internally), never a request URL or header.
  2. A conformance ADVERSARIAL test for ThreadEvent MUST include a fixture exactly like this one
     and assert the producer path (the future `chooseForMe`/`autoMergeCouncil`/gateway-recording
     call sites, THREAD_TOPOLOGY_PLAN.md WP8) REJECTS constructing an event whose `chosenRef` or
     any `alternatives[]` entry matches a credential-shaped pattern
     (`api_key=`, `Bearer\s+[A-Za-z0-9._-]{20,}`, `sk-[A-Za-z0-9-]{20,}`) before the event is
     appended to `dev.aarso.domain.mirror.AarsoEventLog` or written to `delegation_events`.
  3. This applies regardless of `outcome` — redaction/rejection happens before the outcome is
     even considered, exactly as fixtures/common/adversarial/error-secret-leak-in-detail's own
     obligation states for ErrorEnvelope.
  4. No such producer exists yet in this session's scope (WP0/WP1 land only the contract +
     Room-store shapes, not the `chooseForMe` call site) — this fixture documents the obligation
     a later work package's implementation MUST satisfy, per this corpus's "fixtures first"
     Definition of Ready (FB-RAT-DIST-004).
