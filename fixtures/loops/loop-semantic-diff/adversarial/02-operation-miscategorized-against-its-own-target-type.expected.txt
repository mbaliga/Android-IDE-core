Schema validation: PASSES (this document is schema-valid against loop-semantic-diff.schema.json).

Real rejection: violates FB-RAT-LIN-004 ("Lineage comparison reports node, edge, schema,
capability, authority, budget, test, documentation, and compatibility changes separately.",
LOOP_FORK_LINEAGE_CONTRACT.md §8) — again a ratified decision rather than a loop-validation-rules.v1.json
LOOP-* code, since faithful-categorization is not one of that registry's 24 codes; no new code is
invented here per the task instruction.

`op-1`'s own `targetType` field is `"SCHEMA"` and its `description`/`beforeValue`/`afterValue`
unambiguously describe an output-schema change (`outputSchema`'s `summary` field widened to accept
null). Yet `categorizedFindings.documentation` lists `op-1`, while `categorizedFindings.schema` is
empty. A reviewer trusting the categorized report (the whole point of FB-RAT-LIN-004's nine
separate buckets: "never flattened into one undifferentiated change list") would see this as a
harmless documentation edit and skip the schema-compatibility review LOOP_COMPATIBILITY_CONTRACT.md
§7's forward/backward-compatibility rules require for a real schema change — exactly the failure
mode the nine-category split exists to prevent.

Why the schema cannot catch this: `CategorizedFindings` holds `opId` string values with no
structural join back to `SemanticOperation.targetType` on the referenced object. Validating that
every `opId` filed under `categorizedFindings.documentation` actually resolves (via
`semanticOperations[].opId`) to an operation whose own `targetType` is `"DOCUMENTATION"` requires
a cross-array lookup keyed by an opaque ID string — again a validator-time join, not something a
single JSON Schema document can express against itself.
