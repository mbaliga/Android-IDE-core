Schema verdict: VALID against package-fixture-record.schema.json.
recordedOutput is deliberately unconstrained ("shape is whatever that node's own declared output
schema requires") — the schema cannot inspect its contents for secret-like patterns.

Real rejection code: LOOP-PKG-001
  "File '<path under fixtures/>' matches a secret-like pattern (API key, private key, token)."
  Source: schemas/loops/registries/loop-validation-rules.v1.json (LOOP-PKG-001, ERROR severity).
  Remediation: "Remove the secret; secrets are resolved by reference at runtime, never embedded."

docs/ratified/loops/LOOP_PACKAGE_SPEC.md §7 states plainly that validation "MUST scan every file"
for secret-like material — fixtures/ is not exempt from that scan just because its content is
"only" a recorded/mocked value; a fixture is package content like any other file in the archive.
This entry MUST be caught by the gitleaks-based secret scan (§7's named tool) at build and import,
independent of this schema's structural pass.
