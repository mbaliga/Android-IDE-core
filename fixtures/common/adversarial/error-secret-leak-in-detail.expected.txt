Structural validation result: PASSES schemas/common/error.schema.json.
`detail` is only constrained to be a non-empty string — JSON Schema has no way to know a
substring is a live API key, so this fixture is adversarial at the semantic layer only.

Required behavior from a real implementation (not just structural validation):
  1. Before an ErrorEnvelope carrying provider request/response context is constructed,
     the producer MUST scan and redact credential-shaped substrings from `detail`
     (e.g. patterns like `sk-ant-[A-Za-z0-9-]{20,}`, `Bearer [A-Za-z0-9._-]{20,}`,
     `x-api-key:\s*\S+`) per the binding rule "API keys ... never logged" (CLAUDE.md
     rule 5, and FB-RAT-COM-007's technical-detail field is not an exemption from it).
  2. A conformance ADVERSARIAL test for ErrorEnvelope MUST include a fixture exactly
     like this one and assert the producer path REJECTS or REDACTS before persistence/
     transmission — accepting it verbatim is a binding-rule violation, not merely a
     style nit.
  3. Recommended remediation shape: replace the credential substring with a fixed
     redaction marker (e.g. "[REDACTED-KEY]") and keep the rest of `detail` intact, so
     the technical context stays useful without the secret.
  4. This applies regardless of `severity` or `retryable` — redaction happens before
     those fields are even considered.
