<?xml version="1.0" encoding="utf-8"?>
<!--
  Thin shell manifest. Everything else (permissions, activities, services, providers) lives in
  :core-engine's manifest(s) and merges up automatically — duplicating them here would just be
  two sources of truth for the same entries. This file supplies only what genuinely belongs to
  the shipping application: which Application class to instantiate, and the launcher
  icon/label/theme.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="dev.aarso.AarsoApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Aarso" />

</manifest>
