<?xml version="1.0" encoding="utf-8"?>
<!--
  Thin application shell. All permissions, the <application>, the launcher activity, the
  services and the FileProvider come from :core-engine's library manifest, which the
  manifest merger folds in here. applicationId (dev.aarso) is set in build.gradle, so the
  library's ${applicationId} placeholders (e.g. the FileProvider authority) resolve at merge.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
